export interface DataType {
  databaseName: string;
  hostPort: string;
  identity: string;
  address: string;
}
export type FieldType = {
  username?: string;
  password?: string;
  remember?: string;
};
